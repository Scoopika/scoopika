{
  "name": "@scoopika/scoopika",
  "version": "1.0.9",
  "description": "Run AI agents and multi-agent boxes with persistent history and external tools in your application in seconds with real-time streaming hooks, data validation, and auto healing out-of-the-box",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsup --dts-resolve",
    "test": "tsc && rm -r ./dist && npx vitest --watch false --testTimeout 300000",
    "coverage": "vitest run --coverage --testTimeout 500000",
    "prettier": "npx prettier --write ."
  },
  "author": "Kais Radwan",
  "license": "OPEN SCOOP",
  "keywords": [
    "AI",
    "Personalized AI Agents",
    "Multi-agent Boxes",
    "LLMs",
    "Persistent LLMs history"
  ],
  "devDependencies": {
    "@scoopika/types": "^1.8.5",
    "@types/node": "^20.12.7",
    "@vitest/coverage-v8": "^1.5.0",
    "dotenv": "^16.4.5",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.7.0",
    "ajv": "^8.12.0",
    "json-schema-to-ts": "^3.1.0",
    "openai": "^4.33.0"
  }
}
