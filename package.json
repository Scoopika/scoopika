{
  "name": "@scoopika/scoopika",
  "version": "1.0.2",
  "description": "Create magical AI agents and boxes with multiple agents in seconds",
  "main": "dist/src/main.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npx tsup --dts-resolve",
    "test": "tsc && rm -r ./dist && npx vitest --watch false --testTimeout 500000",
    "coverage": "vitest run --coverage --testTimeout 500000",
    "prettier": "npx prettier --write ."
  },
  "author": "Kais Radwan",
  "license": "OPEN SCOOP",
  "keywords": [
    "AI",
    "Agents",
    "AI Boxes"
  ],
  "devDependencies": {
    "@scoopika/types": "^1.2.2",
    "@types/node": "^20.12.7",
    "@vitest/coverage-v8": "^1.5.0",
    "dotenv": "^16.4.5",
    "tsup": "^8.0.2",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.7.0",
    "ajv": "^8.12.0",
    "openai": "^4.33.0"
  }
}
