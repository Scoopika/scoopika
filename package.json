{
  "name": "@scoopika/client",
  "version": "1.0.0",
  "description": "Create magical AI agents and AI boxes",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && npx vitest --watch false --testTimeout 500000",
    "coverage": "vitest run --coverage --testTimeout 500000",
    "prettier": "npx prettier . --write"
  },
  "author": "Kais Radwan",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@vitest/coverage-v8": "^1.5.0",
    "dotenv": "^16.4.5",
    "openai": "^4.33.0",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.7.0"
  }
}
